import AccountantReconciliationTab from '../../tabs/AccountantReconciliationTab';

export default function ReconciliationScreen({ user }: { user: any }) {
  return <AccountantReconciliationTab user={user} />;
} 